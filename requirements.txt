fastapi>=0.95.0
uvicorn[standard]>=0.18.0
pretty_midi>=0.2.11
numpy>=1.24.0
tqdm>=4.60.0
pydantic>=1.10.0
requests>=2.28.0
mangum>=0.17.0
# NOTE: torch is intentionally omitted from this file because the correct
# wheel depends on host CUDA/drivers and Python version. Pass a `TORCH_WHEEL`
# build-arg to the Dockerfile or install torch into your local env manually.